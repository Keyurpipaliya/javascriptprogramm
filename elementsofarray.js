// Write C program to find HCF of two numbers using function.

let prompt = require('prompt-sync') ();

function elementsofarray() {
    console.log("Element of the array: ");
    for (let i = 0; i < arr.length; i++) {
        console.log(arr[i]);
    }
}

function printArray(arr) {
    console.log("Elements of the array :");
    arr.forEach(element => {
        console.log(element);
    });
}

const myJsArray = [10, 20, 30, 40, 50];

printArray(myJsArray);