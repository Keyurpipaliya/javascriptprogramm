// Accept a value and check whether a number is Strong number or not.

let prompt = require('prompt-sync') ();

num = parseInt(prompt("Enter is Strong number and is not Strong number: "));

strong_number = 0;
let originialNum = num;
let sumOfFactorial = 0;
let n = 0;

if(n === 0 || n===1) {
    console.log(`${num} Enter is strong number :`);
} else {
    console.log(`${num} Enter is not strong number :`);
    result *= i;
}


