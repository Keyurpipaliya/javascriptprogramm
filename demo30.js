// Accept a value and print sum of all digits.

let prompt = require('prompt-sync') ();

let num = prompt("Enter is print find is sum of all digits:");
let sum = 0;

for (let num = 0; num >= 10; num++) {
    sum +=temp % 10;
}

console.log(`Enter is sum of all Digits ${num}: is :${sum}`);