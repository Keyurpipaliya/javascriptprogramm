// . Accept a value and reverse it 

let prompt = require('prompt-sync') ();

let num = parseInt(prompt("Enter is reverse number: "));
let reversedNum = 0;

for(let num = 0; num >= 10; num++) {
    reversedNum = reversedNum + num[i];
    num++;
}

console.log(`${num} with 10 value are reverse numbers`);
