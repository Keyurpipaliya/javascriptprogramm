// Accept a value and print sum of second and second last digit.

let prompt = require('prompt-sync') ();

let num = parseInt(prompt('Enter is sum of second and second last digit: '));

let lastDigit = [];

if(num.length < 2) {
    console.log('The number must have at least two digits.');
} else {
    let secondDigit = parseInt(num[1]);
}

for (let i = 0; i < num.length; i++) {

    let sum = seconddigit + secondlastDigit;

    console.log(`The sum of second and second last digit number:`);
}